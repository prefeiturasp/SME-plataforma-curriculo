version: '3'

services:
  interface:
    volumes:
      - ./interface:/app
    ports:
      - 5000
    environment:
      - APP_ENV=development

  api:
    volumes:
      - ./api:/app
    ports: 
      - 8666